---
name: defect-fixer
description: |
  根据用户描述的缺陷/问题进行根因分析、提出修复方案，在用户确认后执行代码修复，
  并同步修正存在缺陷的设计文档。
  
  触发场景：
  (1) 用户报告 bug、错误、异常行为，附带了错误信息/traceback/日志；
  (2) 用户引用 module-implementation-review 审查报告中的具体问题条目；
  (3) 用户口头描述某个功能"不对"、"有问题"、"返回值错误"等；
  (4) 用户要求修复测试失败、linter 报错、运行时崩溃等问题；
  (5) 用户提及"修 bug"、"修一下"、"这个有问题"、"fix this"、"缺陷"等关键词。
  
  核心特点：先诊断、后提案、循环确认、再动手。不止修复代码——
  如果缺陷根源是设计文档本身的错误，会同步修正设计文档。
---

# 缺陷修复器

## 核心工作流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ 1.理解缺陷    │ →  │ 2.根因分析    │ →  │ 3.提案确认    │
│  定位相关代码  │    │  判断缺陷性质  │    │  循环直到同意  │
└──────────────┘    └──────────────┘    └──────┬───────┘
                                               │ 用户明确同意
                    ┌──────────────┐    ┌──────▼──────┐
                    │ 5.汇报结果    │ ←  │ 4.执行修复    │
                    └──────────────┘    └─────────────┘
```

### 步骤 1：理解缺陷，定位相关代码与文档

用户以任意形式描述缺陷。根据输入类型采取不同的切入方式：

**A. 报错信息 / traceback**：
- 从 traceback 中提取文件路径、行号、异常类型
- 直接读取出错文件的相关代码
- 分析调用链，理解触发条件

**B. 审查报告条目**（来自 module-implementation-review）：
- 解析条目中的文件路径、行号、问题描述
- 直接定位到对应代码位置
- 将审查报告中的问题描述作为已知结论使用

**C. 口头描述**（"XX 功能返回值不对"）：
- 从描述中提取关键实体（函数名、模块名、数据结构）
- grep 搜索相关代码
- 阅读核心逻辑，对比预期行为与当前实现

**定位设计文档**：
1. 优先查找项目中的 `功能模块全拆解.md`（由 module-breakdown-designer 生成），根据缺陷涉及的功能自动匹配最相关的模块编号
2. 根据模块编号找到对应的功能设计文档（在 `docs/功能设计/`、`docs/specs/` 等目录下）
3. 若项目无拆解文档，直接搜索 `docs/` 下文件名或内容与缺陷关键词相关的设计文档
4. 读取设计文档中与该缺陷相关的章节（核心逻辑步骤、异常处理、边界条件等）

**重要**：如果仅凭用户描述无法定位到具体代码或文档，先向用户追问关键信息（文件路径、复现步骤、预期行为），再进行下一步。

### 步骤 2：根因分析

定位到相关代码后，深入分析缺陷的根本原因。对照设计文档（如有）判断缺陷性质：

| 缺陷性质 | 定义 | 设计文档 |
|:---|:---|:---|
| **实现 bug** | 代码行为与设计文档不一致（逻辑错误、边界遗漏、类型误用等） | 设计文档是正确的，不需要改 |
| **设计缺陷** | 设计文档本身的逻辑/约束/接口定义有误，代码只是按错误设计实现 | 设计文档是错的，需要同步修正 |

**分析过程保持简洁**：
- 向用户呈现根因时，用 3-5 句话说明：问题出在哪、为什么会发生、影响范围
- 不要输出长篇排查日志，只给结论和关键证据（文件路径、行号、关键变量值）
- 若根因无法确定，诚实说明不确定性，提出需要用户补充的信息

### 步骤 3：提案确认（循环直到用户明确同意）

根据根因分析结果提出修复方案。

**方案呈现格式**：
```
## 根因
{2-3 句话说明根本原因}

## 修复方案
{具体的修改计划，包括：}
- 修改文件：{路径}
- 修改内容：{简述改动内容}
- {若涉及设计文档} 设计文档同步：{文档路径 + 修改内容}

## 影响范围
{一句话说明是否影响其他模块}

## 测试建议
{若适合补充回归测试} 建议补充测试：{测试场景描述}
{若不合适} 不适合自动化测试，原因：{简述}
```

**确认循环规则**：
- 用户可能同意、否定、或提出修正意见
- 若用户不同意：根据用户反馈调整方案，重新呈现，回到循环
- 若用户提出修正意见：修改方案，重新呈现，确认是否满足预期
- **仅在用户明确表示同意时**（如"可以"、"OK"、"就这样修"、"同意"），才进入步骤 4
- 若用户表述模糊（如"应该可以吧"、"你看着办"），追问一次确认："请确认是否按此方案修复？"

### 步骤 4：执行修复

仅在用户明确同意后执行。按以下顺序操作：

1. **修复代码**：按确认的方案修改代码文件
2. **同步设计文档**（仅设计缺陷）：如果根因是设计文档错误，同步修正设计文档中的相关章节（逻辑步骤、异常策略、边界条件、验收测试等）。若修正涉及多个文档，逐个修改
3. **补充回归测试**（如适用）：如果 bug 适合用自动化测试覆盖（纯逻辑错误、边界遗漏、异常处理缺失等），补充针对该场景的测试用例。不适合的情况包括：环境相关 bug、UI 渲染问题、竞态条件等难以自动化验证的场景
4. **自检**：修复后快速检查：
   - 修复是否完整覆盖了根因
   - 是否引入了新的问题（如未使用的 import、破坏的接口）
   - 若涉及设计文档修改，代码与文档是否一致

### 步骤 5：汇报结果

```
## 缺陷修复完成

### 根因
{一句话}

### 修改内容
- 代码：{文件列表 + 关键改动}
- 设计文档：{文件列表 + 关键改动，如无则写"无"}
- 测试：{补充的测试，如无则写"不适合补充"}

### 建议
{如有后续注意事项，一句话说明；无则省略}
```

## 关键原则

- **先诊断、后动手**：不要在没搞清楚根因的情况下直接改代码。步骤 1-2 是必要的前置
- **区分缺陷性质**：实现 bug 不改设计文档，设计缺陷才改。不确定时标注为"待确认"向用户提问
- **精准修复**：只修改与缺陷直接相关的代码，不趁机重构无关代码
- **尊重用户决策**：用户是最终的决策者，方案必须经用户确认才能执行。即使用户选择的方案不是最优解，也按用户决定执行（但可以温和提醒风险）
- **证据驱动**：根因分析给出具体证据（行号、变量值、调用链），不说"可能是 XX 的问题"

## 边界情况

- **缺陷跨多个模块**：逐一分析每个受影响模块，在方案中合并呈现，一次确认后统一修复
- **缺陷无法定位**：向用户追问复现步骤、输入数据、环境信息等，不要猜测
- **修复可能引入破坏性变更**：在方案中明确标注"⚠️ 破坏性变更"，说明影响面和迁移路径
- **设计文档不存在**：仍进行根因分析和代码修复，在汇报中注明"未找到相关设计文档，未进行文档同步"
- **用户要求跳过分析直接修**：温和提醒先分析更稳妥，若用户坚持，按用户要求执行但注明"未进行完整根因分析"
