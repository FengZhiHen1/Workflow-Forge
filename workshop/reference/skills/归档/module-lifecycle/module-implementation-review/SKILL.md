---
name: module-implementation-review
description: |
  根据用户指定的模块编号，审查对应功能模块的代码落地情况与模块间联动实现度。
  对照功能设计文档检查：交付物完整性、接口实现、核心逻辑正确性、测试覆盖。
  每个模块的设计规格由两份文档组成：`-设计文档.md`（项目上下文）和 `-落地规范.md`（精确编码规格）。
  审查时以落地规范为主要对照标准，设计文档用于补充理解模块边界与依赖关系。
  同时验证多个模块之间是否按设计完成联动（数据流、调用链、事件传递等）。
  最终将审查结果保存为 Markdown 报告到项目的 docs/ 审查目录下。
  
  使用场景：
  (1) 用户给出模块编号（如 M01 M03 M05），要求审查这些模块的实现情况；
  (2) 用户要求检查某几个模块之间是否能正常联动/协作；
  (3) 用户要求对照设计文档验证代码落地度；
  (4) 用户提到"模块审查"、"联动检查"、"实现度验收"、"模块验收"等关键词。
  
  触发条件：用户输入包含模块编号（如 M01、模块1、Module-A 等），或明确要求审查模块实现/联动。
---

# 模块实现度与联动审查

## 前置条件

- 用户已明确指定一个或多个模块编号
- 项目存在 `docs/` 目录（或类似目录）存放功能设计文档

## 工作流程

### 1. 识别模块编号

从用户输入中提取所有模块编号。常见格式：
- `M01`, `M02`, `M1`, `M2` 等字母+数字格式
- `模块1`, `模块2` 等中文格式
- `Module-A`, `Module-B` 等英文格式

如用户未明确给出编号但提到"所有模块"或"全部模块"，需先扫描 `docs/功能设计/` 或 `docs/specs/` 目录列出所有模块编号，再请用户确认。

### 2. 定位功能设计文档

每个模块的设计规格由两份文档组成：`[编号]-[名称]-设计文档.md` 和 `[编号]-[名称]-落地规范.md`。按以下优先级顺序查找：

**第一优先：独立模块文档（双文件）**
- 扫描 `docs/功能设计/`、`docs/specs/`、`docs/modules/`、`docs/design/` 等目录
- 文件名包含模块编号且以 `-设计文档.md` 或 `-落地规范.md` 结尾
- 如 `M01-用户认证-设计文档.md`、`M01-用户认证-落地规范.md`
- 使用 glob + 大小写不敏感匹配
- **两份文档都必须定位**——设计文档提供模块边界和依赖关系，落地规范提供精确的接口契约和测试场景

**第二优先：旧版单文件文档（向后兼容）**
- 若无双文件，查找旧版单文件：`[编号]-[名称].md`（如 `M01-用户管理.md`）
- 旧版文档同时包含设计描述和编码规格，直接作为唯一对照源

**第三优先：总设计文档中的章节**
- 查找 `docs/` 下的总设计文档（如 `总设计.md`、`架构设计.md`、`功能模块全拆解.md`）
- 在文档中搜索模块编号对应的章节（通常以 `## M01` 或 `### 模块1` 开头）
- 提取该章节内容作为该模块的设计规格

**第四优先：其他可能位置**
- `README.md` 中是否包含模块设计信息
- `DESIGN.md`、`ARCHITECTURE.md` 等根目录文档

> 若某模块找不到任何设计文档，记录为 "⚠️ 缺失设计文档"，仍继续检查代码存在性。若仅找到设计文档或仅找到落地规范，记录为 "⚠️ 设计文档不完整——缺少设计文档/落地规范"。

### 3. 提取模块规格与联动关系

对每个模块，从两份文档中分别提取信息：

**从落地规范提取（编码规格）**：
- 交付物清单：预期文件路径、核心类/函数/接口名、数据模型定义
- 接口契约：输入参数及类型、返回值及类型、异常/错误码定义、边界条件与约束

**从设计文档提取（项目上下文）**：
- 模块边界：依赖哪些模块（调用谁）、被谁依赖（被谁调用）
- 已有设计兼容性分析：已有代码文件路径、复用的已有类型
- 设计约束与易错点

> 若是旧版单文件文档，所有信息从同一文件中提取。

**联动关系**（从两份文档中综合提取）：
- 该模块**依赖**哪些模块（调用谁）
- 该模块**被谁依赖**（被谁调用）
- 数据流方向（输入来源、输出去向）
- 事件/消息传递关系
- 共享数据/状态

> 联动关系在设计文档的"依赖关系概述"和落地规范的"依赖与集成接口"中均有描述，前者提供宏观关系，后者提供精确接口。

### 4. 代码落地检查

对每个模块的交付物清单逐项验证：

| 检查项 | 方法 |
|--------|------|
| 文件是否存在 | `glob` 或文件系统检查 |
| 文件非空 | 文件大小 > 0 |
| 语法有效 | `python -m py_compile`、`tsc --noEmit` 等 |
| 核心符号存在 | `grep` 搜索类名/函数名 |
| 接口签名匹配 | 对比参数列表、返回类型 |
| 核心逻辑实现 | 读取代码，检查是否有 TODO/占位符 |
| 测试覆盖 | 查找对应测试文件，运行测试 |

**占位符处理**：如果文件仅包含 TODO、pass、空函数等占位内容，视为"⚠️ 部分实现"而非"✅ 已完成"。

### 5. 联动验证（跨模块）

对用户指定的**所有模块组合**，进行联动检查：

**步骤 A：构建预期联动图**
- 汇总各模块设计文档中提取的联动关系
- 形成有向图：节点=模块，边=依赖/调用/数据流
- 识别需要验证的联动路径（限于用户指定的模块范围内）

**步骤 B：验证实际联动**
- **代码静态分析**：通过 `grep` 检查模块 A 是否实际 import/调用了模块 B
- **接口匹配**：检查模块 A 调用的接口签名是否与模块 B 提供的接口一致
- **数据流验证**：检查模块间传递的数据结构是否一致（如模块 A 输出 DTO 与模块 B 输入 DTO 是否匹配）
- **配置/路由检查**：如果是 Web/API 项目，检查路由注册、服务发现等是否将模块正确连接

**步骤 C：标记联动状态**
- ✅ 已实现：设计文档描述的联动在实际代码中存在且正确
- ⚠️ 部分实现：联动存在但接口不完全匹配，或有降级处理
- ❌ 未实现：设计文档描述的联动在代码中找不到证据
- ❓ 无法验证：代码经过动态组装（如依赖注入、配置驱动），静态检查无法确认

### 6. 生成审查报告

报告采用"**摘要 + 详细附录**"双栏结构，保存到项目的审查结果目录。

**获取准确时间戳**：

在写入报告的**审查时间**和**版本记录**时，**禁止直接依赖系统环境变量或 AI 内部时间推断**，应调用本 Skill 提供的跨平台脚本获取准确时间：

```bash
# 标准格式（用于报告中的审查时间和版本记录）
python <skill-root>/scripts/get_timestamp.py
# 输出示例：2026-04-28 20:06:57

# ISO 8601 格式（用于文件名或版本号）
python <skill-root>/scripts/get_timestamp.py --iso
# 输出示例：2026-04-28T20:06:57+08:00
```

> **说明**：该脚本兼容 Windows / Linux / macOS，自动处理时区（固定为 Asia/Shanghai / CST），无需额外依赖（Python 3 内置）。

**确定保存位置**：
- 查找 `docs/` 下是否已有 `审查报告/`、`reviews/`、`audit/` 等目录
- 如有，保存到该目录下，文件名为 `模块审查-<模块编号列表>-<日期>.md`
- 如无，在 `docs/` 下创建 `审查报告/` 目录再保存

**报告结构**见 [references/report-template.md](references/report-template.md)。报告中必须包含：
- **审查时间**：由脚本输出的准确时间（精确到秒）
- **版本记录**：记录本次审查的版本号、时间、审查人、变更摘要。如果该模块已有历史审查报告，继承其版本号并递增（如 v1.0 → v1.1）
- **冲突核查指引**：若本次审查结论与历史报告冲突，优先以时间戳更新的版本为准，并在版本记录中追加冲突解决条目。

### 7. 输出摘要给用户

向用户汇报时，优先给出总体结论和关键问题，不要一次性输出完整报告内容。建议格式：

```
📋 模块审查完成（M01, M03, M05）

总体结论：3个模块中，2个基本完成，1个存在关键缺口。

🔴 需关注：
- M03 的 XX 功能尚未实现（设计文档要求...，实际代码...）
- M01 → M03 的数据流联动未建立（M03 未调用 M01 提供的接口）

🟡 建议优化：
- M05 的测试覆盖率偏低（仅 2 个测试用例）

详细报告已保存至：docs/审查报告/模块审查-M01-M03-M05-2026-04-30.md
```

## 参考资源

- **审查清单**：执行检查时参考 [references/review-checklist.md](references/review-checklist.md)
- **报告模板**：完整报告结构见 [references/report-template.md](references/report-template.md)
- **联动模式参考**：常见模块联动模式见 [references/integration-patterns.md](references/integration-patterns.md)

## 注意事项

1. **不做代码修复**：本 Skill 仅负责审查和报告，不自动修改代码。如需修复，明确告知用户并请求确认。
2. **区分"未实现"和"设计未要求"**：只检查设计文档明确要求的交付物，不要以"我觉得还缺什么"为由增加检查项。
3. **联动范围限制**：联动检查仅聚焦于用户指定的模块集合。如果设计文档提到模块依赖了用户未指定的模块，仅记录为"外部依赖"，不深入检查。
4. **中文优先**：所有报告、输出使用中文，保留代码中的英文符号名。
5. **保留证据**：报告中的每个结论都应有对应证据（文件路径、行号、引用片段），避免主观判断。
