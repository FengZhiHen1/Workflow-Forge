# 项目全局状态规范 v3.0.0

---

## 〇、设计决策

**不引入新持久文件。** 项目级全局状态是派生视图——`wfctl status` 每次调用时扫描 `.agent/instances/` 下所有 instance.json 实时聚合。单一事实来源（instance.json），无同步延迟、无写竞争。

---

## 一、`wfctl status` 双模式

| 调用 | 行为 |
|------|------|
| `wfctl status` | 扫描所有 instance.json，返回项目级聚合摘要 |
| `wfctl status --instance <id>` | 返回指定实例摘要（已有，见 wfctl 接口规范 §二） |

---

## 二、项目级返回结构

```json
{
  "active_instances": [
    {
      "instance_id": "20260517-001",
      "workflow_id": "math-model",
      "status": "ACTIVE",
      "stages_done": 3,
      "stages_total": 10,
      "blocked_by": [
        {"stage_id": "s04", "status": "AWAITING_CONFIRM", "output_message_id": "msg-014"}
      ]
    }
  ],
  "recent_completed": ["20260516-003", "20260516-002"],
  "recent_failed": ["20260516-001"]
}
```

| 字段 | 说明 |
|------|------|
| `active_instances` | 当前所有 `ACTIVE` 实例。含进度计数和阻塞点摘要 |
| `blocked_by` | 该实例中所有非 DONE 的非 RUNNING 阻塞 stage（AWAITING_CONFIRM、ERROR、CONFLICT、以及所有依赖未满足的 PENDING）。每个阻塞点含 `stage_id`、`status`、`output_message_id`——开发者直接拿 message_id 定位问题 |
| `recent_completed` | 最近完成的实例 ID，最多 5 条，按时间倒序 |
| `recent_failed` | 最近失败的实例 ID，最多 5 条，按时间倒序 |

### 最小化原则

不做模块依赖图、不做详细进度百分比、不做实例间父子关系树。这些信息可按需通过 `--instance <id>` 定向查询。待实践沉淀出真实需求后再扩展。

---

## 三、主 Agent 查询策略

主 Agent 持有两层视图：

| 层 | 命令 | 内容 | 使用场景 |
|----|------|------|---------|
| 全局视野 | `wfctl status` | 项目摘要 | 用户问"现在什么进度"、主 Agent 判断有无活跃实例 |
| 实例详情 | `wfctl status --instance <id>` | 单实例完整摘要 | 排查阻塞点、查看子工作流状态 |

主 Agent 不缓存——需要时调 wfctl，用完即弃。符合 wfctl 无状态模型。

---

## 四、状态流转与视图更新

`active_instances` / `recent_*` 是派生视图，自动反映 instance.json 的变更，无需额外更新规则：

| 实例状态变更 | 视图表现 |
|-------------|---------|
| `create` 后 | 出现在 `active_instances` |
| `ACTIVE` 期间 stage 推进 | `stages_done` 递增，`blocked_by` 动态变化 |
| `COMPLETED` | 从 `active_instances` 移除，加入 `recent_completed` |
| `FAILED` | 从 `active_instances` 移除，加入 `recent_failed` |

`recent_completed` / `recent_failed` 仅保留最近 5 条 ID——wfctl `status` 扫描时按 instance.json 的最终时间戳排序截断。完整的实例历史由 `.agent/instances/` 目录下的 instance.json 自身承载——`recent_*` 只是最近入口，不是完整审计日志。
