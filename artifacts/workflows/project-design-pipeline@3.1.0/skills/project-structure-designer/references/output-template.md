# 项目结构设计文档输出模板

本参考文件定义 `项目名称-项目结构.md` 的精确结构与质量标准。

## 文件命名

- **新建**：`docs/项目名称-项目结构.md`
- **增量更新**：覆盖原文件，并在版本记录中追加一行

---

## 文档结构

```markdown
# [项目名称] - 项目结构设计

> 生成日期：YYYY-MM-DD
> 技术栈方案：[项目名称-技术栈设计.md]
> 架构模式：[选定的架构模式]

---

## 版本记录

| 日期 | 版本 | 变更内容 | 关联技术栈 |
|------|------|----------|-----------|
| YYYY-MM-DD | v1.0 | 初始创建 | [技术栈文档链接] |

---

## 一、项目基本信息

- **项目名称**：[名称]
- **项目类型**：[前端 / 后端 / 全栈 / Monorepo / 库]
- **核心功能**：[3-5 条]
- **目标平台**：[Web / H5 / 桌面 / 移动端 / 全平台]
- **预期规模**：[小型 MVP / 中型 / 大型]
- **团队规模**：[建议人数与角色分工]
- **代码组织风格**：[Feature-Based / Layer-Based / 混合 / 领域驱动]

---

## 二、技术栈回顾

用 3-5 行摘要回顾技术栈方案的关键选型，为后续结构设计提供上下文：

| 维度 | 选型 | 对结构的影响 |
|:---|:---|:---|
| 前端框架 | [选型] | [目录范式、组件组织方式] |
| 后端语言/框架 | [选型] | [包组织方式、入口文件约定] |
| 数据存储 | [选型] | [数据访问层组织] |
| 架构模式 | [选型] | [顶层分层策略] |
| 部署方式 | [选型] | [是否需要 Docker/K8s/CI 配置目录] |

---

## 三、设计原则

列出 3-5 条本项目结构遵循的核心原则：

- [如：职责分离——每层只做自己该做的事]
- [如：依赖倒置——高层不依赖低层实现细节]
- [如：可测试性——各层可独立进行单元测试]
- [如：扩展开放——新增功能通过新增模块而非修改现有模块实现]

---

## 四、选定的结构模式

记录步骤 2 的模式选择结果与理由：

| 维度 | 选定模式 | 选择理由 | patterns-by-stack.md 章节 |
|:---|:---|:---|:---|
| 前端 | [如 React Feature-Based] | [中大型项目、团队 >3 人、长期维护] | §1 模式 A |
| 后端 | [如 FastAPI 三层架构] | [标准 Web 项目、领域复杂度适中] | §3 模式 A |
| Monorepo | [如按应用边界分层] | [前后端分离、共享类型/配置库] | §6 模式 A |
| Python 包 | [如 src-layout] | [需 pip install -e . 开发安装] | §7 |

> 若某维度不适用（如非 Monorepo 项目），标注"不适用"即可。

---

## 五、分层架构

### 5.1 分层总览

| 层级 | 层名 | 职责 | 典型内容 | 技术组件 |
|:---:|:---|:---|:---|:---|
| L1 | 表示层 | 用户界面与交互 | 页面、组件、路由 | [前端框架] |
| L2 | 应用层 | 业务用例编排 | Service、DTO、Middleware | [后端框架] |
| L3 | 领域层 | 核心业务逻辑 | Entity、ValueObject、Repository接口 | — |
| L4 | 基础设施层 | 外部依赖适配 | Repository实现、API Client、Cache | [数据库驱动] |
| L5 | 公共层 | 跨层共享 | 类型定义、工具函数、常量 | — |

### 5.2 层间依赖规则

用 Mermaid 图或文字描述层间允许的依赖方向：

```
表示层 → 应用层 → 领域层 ← 基础设施层
                    ↑
                公共层（所有层可引用）
```

### 5.3 各层详解

对每一层展开（逐层一节）：
- **职责边界**：该层负责什么、不负责什么
- **包含内容**：具体的文件/目录类型
- **对外接口**：该层向上暴露什么、向下依赖什么
- **技术映射**：该层使用的具体技术栈组件

---

## 六、目录骨架

### 6.1 完整目录树

```
project-root/
├── src/
│   ├── presentation/        # L1 表示层
│   │   ├── pages/           #   页面入口（路由级别）
│   │   ├── components/      #   可复用 UI 组件
│   │   └── layouts/         #   布局组件
│   ├── application/         # L2 应用层
│   │   ├── services/        #   用例编排服务
│   │   ├── dto/             #   数据传输对象
│   │   └── middleware/      #   请求级中间件（认证、日志）
│   ├── domain/              # L3 领域层
│   │   ├── entities/        #   业务实体
│   │   ├── value-objects/   #   值对象
│   │   └── repositories/    #   仓储接口（仅接口定义）
│   ├── infrastructure/      # L4 基础设施层
│   │   ├── persistence/     #   数据库实现（ORM、Migration）
│   │   ├── external/        #   外部 API 客户端
│   │   └── cache/           #   缓存实现
│   └── shared/              # L5 公共层
│       ├── types/           #   共享类型/接口定义
│       ├── utils/           #   纯函数工具
│       └── constants/       #   全局常量
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── docs/
├── scripts/
└── [框架配置文件]
```

> 目录树应根据实际技术栈和步骤 2 选定的结构模式调整。示例为 Clean Architecture 通用分层模板。若选定模式为 React Feature-Based、FastAPI 三层、Go 标准分层等，应使用 `references/patterns-by-stack.md` 中对应模式的目录结构替换此示例。

### 6.2 目录职责速查表

| 目录 | 所属层 | 职责 | 依赖方向 |
|:---|:---|:---|:---|
| `src/presentation/` | L1 | UI 渲染与用户交互 | 可引用 L2、L5 |
| `src/application/` | L2 | 用例编排与流程控制 | 可引用 L3、L5 |
| `src/domain/` | L3 | 核心业务规则 | 仅可引用 L5 |
| `src/infrastructure/` | L4 | 外部系统适配 | 实现 L3 定义的接口，可引用 L5 |
| `src/shared/` | L5 | 跨层共享基础设施 | 不被任何层依赖（仅被引用） |

---

## 七、层间数据流

选取 2-3 个代表性业务场景，描述数据在各层之间的流转。

### 7.1 场景一：[名称]

```
用户请求 → L1(接收HTTP请求、参数校验)
         → L2(调用应用服务、组装上下文)
         → L3(执行领域逻辑、返回Entity)
         → L2(组装DTO)
         → L1(渲染响应)
```

数据形态变化：`HTTP Request → Command/Query DTO → Domain Entity → Response DTO → HTTP Response`

### 7.2 场景二：[名称]

[重复格式]

---

## 八、模块间依赖关系

描述主要模块/package 之间如何协作，可使用 Mermaid 图：

```mermaid
graph LR
    A[模块A] --> B[模块B]
    B --> C[模块C]
```

列出各模块的职责摘要：

| 模块 | 职责 | 依赖 | 被依赖 |
|------|------|------|--------|
| [模块A] | [职责] | [依赖哪些模块] | [被哪些模块依赖] |

---

## 九、特殊文件说明

| 文件名 | 位置 | 作用 | 注意事项 |
|--------|------|------|----------|
| [文件1] | [路径] | [说明] | [注意点] |
| [文件2] | [路径] | [说明] | [注意点] |

---

## 十、包/模块组织约定

### 10.1 命名规范

| 元素 | 命名风格 | 示例 |
|:---|:---|:---|
| 目录 | [kebab-case / snake_case] | `user-profile/` |
| 文件 | [风格] | `user-service.ts` |
| 类/接口 | [PascalCase] | `UserService` |
| 函数/变量 | [camelCase / snake_case] | `getUserById` |
| 常量 | [UPPER_SNAKE_CASE] | `MAX_RETRY_COUNT` |

### 10.2 文件组织规则

- 每个文件只导出一个主要的类/函数/组件
- 通过 barrel 文件（`index.ts` / `__init__.py`）统一对外暴露
- 测试文件与被测文件同目录或镜像到 `tests/` 目录
- 禁止跨层 import（基础设施层 import 表示层等）

### 10.3 模块边界

若为 monorepo：
- 各 package 的 `package.json` / `pyproject.toml` 明确声明依赖
- 共享代码通过 npm workspace / pip editable install 引用
- 禁止 package 间循环依赖

---

## 十一、测试与文档规划

- **单元测试**：放置于 `tests/unit/`，与被测模块镜像目录结构，使用 [测试框架]
- **集成测试**：放置于 `tests/integration/`，覆盖跨模块/跨层交互
- **E2E 测试**：放置于 `tests/e2e/`，覆盖关键用户旅程
- **测试数据/夹具**：放置于 `tests/fixtures/` 或与被测文件同目录
- **文档**：`docs/` 下按类型分子目录（设计文档 / API 文档 / 运维文档）

---

## 十二、扩展预留

为未来可能引入但尚未在技术栈方案中确定的技术，预留目录位置：

| 预留位置 | 预期用途 | 触发条件 |
|:---|:---|:---|
| `src/infrastructure/queue/` | 消息队列适配 | 异步任务需求确认后 |
| `src/infrastructure/search/` | 全文搜索引擎适配 | 搜索需求确认后 |
| `docker/` | 容器化配置 | 正式部署前 |

> 扩展预留仅创建空目录（含 `.gitkeep`），不预先编写代码。

---

## 十三、最佳实践说明

解释该结构遵循的核心最佳实践及其原因：

- [如：Clean Architecture 依赖规则——内层不依赖外层，确保核心业务逻辑与技术细节解耦]
- [如：Feature-Based 组织——按业务功能而非技术类型分组，降低跨功能修改的认知负担]
- [如：Barrel 导出模式——每个目录通过 index 文件统一暴露，外部只需 import 目录路径]

---

## 附录 A：技术栈映射速查

| 技术栈组件 | 所属层 | 对应目录 |
|:---|:---|:---|
| [逐一列出技术栈方案中的每个组件] | | |

## 附录 B：设计决策记录

记录结构设计过程中的关键决策：
- 为什么选择此分层策略而非其他？
- 为什么某些目录合并而非拆分？
- 哪些决策是受技术栈约束而做出的？
```

---

## 输出检查清单

完成文档前请自检：

1. [ ] 是否区分了新建与增量更新场景？
2. [ ] 增量更新时，是否保留了原文档中仍有效的结构决策？
3. [ ] 项目基本信息是否填写完整？
4. [ ] 设计原则是否与技术栈方案和架构模式一致？
5. [ ] 分层架构是否覆盖了项目实际需要的所有层？
6. [ ] 目录树是否反映了步骤 2 选定的结构模式（而非套用通用模板）？
7. [ ] 层间数据流是否覆盖了至少 2 个代表性场景？
8. [ ] 模块依赖关系图是否准确、无循环？
9. [ ] 特殊文件（入口文件、配置文件）是否已标注？
10. [ ] 是否标注了关键约束（分层规则、禁止跨层调用）？
11. [ ] 是否包含测试文件放置策略和测试框架选型？
12. [ ] 是否包含扩展预留（含触发条件）？
13. [ ] 文件路径是否符合命名规范？
14. [ ] 附录 B 是否记录了关键设计决策及其理由？
