# MinerU Precision Extract API 配置
# 复制此文件为 .env 并填入实际值

# API Token（必填）。从 https://mineru.net 获取
MINERU_TOKEN=your_token_here

# 模型版本（可选，默认 vlm）
# 可选值: vlm | pipeline | MinerU-HTML
MINERU_MODEL_VERSION=vlm

# 轮询间隔（秒，可选，默认 3）
MINERU_POLL_INTERVAL=3

# 轮询超时（秒，可选，默认 300）
MINERU_POLL_TIMEOUT=300
