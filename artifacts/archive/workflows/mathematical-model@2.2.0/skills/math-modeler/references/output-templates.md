# Reference：输出文件模板与命名规范

本文件定义 math-modeler 的全部输出文件模板。Skill 主体中仅保留文件清单与引用说明，**详细模板内容以此文件为准**。

---

## 文件组织方式

每个小问的数学建模产出拆分为 **N 个独立文件**（N 根据模型复杂度动态确定，至少 3 个）：

| 序号 | 文件路径 | 内容 | 是否必须 |
|:---|:---|:---|:---|
| 1 | `VERSION_DOCS/P3-符号体系与假设_[model].md` | 统一符号表 + 假设五维评估 + 数据映射表 | ✅ |
| 2 | `VERSION_DOCS/P3-公式推导_问题建立_[model].md` | 从问题到目标函数/控制方程的完整推导 | ✅ |
| 3 | `VERSION_DOCS/P3-公式推导_求解算法_[model].md` | 从控制方程到数值解的推导 + 可解性分析 | ✅（若需数值求解）|
| 4 | `VERSION_DOCS/P3-误差与敏感性分析_[model].md` | 误差预算 + 敏感性矩阵 + 结构敏感性 | ✅ |
| 5 | `VERSION_DOCS/P3-验证标准_[model].md` | 可检验性指标、阈值、Toy Model 验证结果 | 推荐 |

> 命名规范：`[model]` 必须与 manifest.yaml 中登记的 `model` 字段一致。例如 model=ARIMA 时，文件为 `P3-公式推导_问题建立_ARIMA.md`。

---

## 模板一：符号体系与假设（`P3-符号体系与假设_[model].md`）

```markdown
# [模型名] 符号体系与假设评估报告

## 版本记录
| 日期 | 版本 | 修改内容 | 修改原因 |
| :--- | :--- | :--- | :--- |
| YYYY-MM-DD | v1.0 | 初稿 | 基于选型报告构建符号体系与假设评估 |

## 1. 符号体系

### 1.1 确定变量
| 符号 | 含义 | 量纲 | 取值范围 | 代码变量 | 维数声明 |
|:---|:---|:---|:---|:---|:---|
| $x$ | ... | ... | ... | `x` | 标量 |
| $\mathbf{X}$ | ... | ... | ... | `X_mat` | $n \times p$ 矩阵 |

### 1.2 随机变量
| 符号 | 含义 | 分布假设 | 代码变量 | 备注 |
|:---|:---|:---|:---|:---|
| $\varepsilon$ | 随机误差 | $N(0, \sigma^2)$ | `epsilon` | 独立同分布 |

### 1.3 索引约定
- 时间索引：$t = 0, 1, 2, \dots, T$（从 0 开始）
- 空间/样本索引：$i = 1, 2, \dots, n$（从 1 开始）
- 离散化网格：边界点包含 / 不包含

## 2. 模型假设五维评估

### 假设 1：[假设名称]
- **内容**：...
- **合理性**：...（评级：高/中/低，证据：...）
- **必要性**：...（是否核心假设：是/否）
- **局限性**：...（失效条件：...）
- **优先级**：P0/P1/P2
- **退化路径**：若去掉该假设，模型修正为 ...（可指向 model-architect 备选方案）

### 假设 2：...
...（同上结构）

### 隐式假设挖掘
- 隐式假设 1：...（题目未明说但推导中默认成立）
- ...（同上五维评估结构）

## 3. 数据-公式五层映射表

| 数学符号 | 代码变量名 | 数据字段名 | 预处理要求 | 单位/量纲 | 实现约束 |
|:---|:---|:---|:---|:---|:---|
| ... | ... | ... | ... | ... | ... |

## 4. 数据质量评估

### 4.1 样本量充足性
- 模型自由度：$k = ...$
- 建议最小样本量：$n_{\min} = ...$（依据经验法则或理论下界）
- 实际可用样本量：$n = ...$
- 结论：充足 / 临界 / 不足

### 4.2 数据质量诊断
- 信噪比估计：SNR ≈ ...
- 多重共线性：最大 VIF = ... / 条件数 $\kappa = ...$
- 缺失模式：MCAR / MAR / MNAR，缺失率 = ...%

## 5. 奠基文献
- [作者, 年份] 《标题》
- [作者, 年份] 《标题》
```

---

## 模板二：公式推导-问题建立（`P3-公式推导_问题建立_[model].md`）

```markdown
# [模型名] 公式推导报告 — 问题建立

> 本推导基于 `P3-符号体系与假设_[model].md` 中的符号定义与假设体系。

## 1. 问题重述
- 用数学语言重新描述赛题小问的目标
- 明确决策变量、状态变量、参数、已知条件

## 2. 逐步推导

### 2.1 [步骤名，如"目标函数构建"]
**公式 (2.1)**：$$
...（LaTeX 公式）
$$
**推导依据**：基于 [假设 X / 已知条件 Y / 引理 Z]
**说明**：...

### 2.2 [步骤名，如"约束条件推导"]
**公式 (2.2)**：$$
...（LaTeX 公式）
$$
...

## 3. 完整数学模型

将上述推导汇总为标准的数学规划/方程形式：

$$
\begin{aligned}
\min_{\mathbf{x}} \quad & f(\mathbf{x}) \\
\text{s.t.} \quad & g_i(\mathbf{x}) \leq 0, \quad i = 1, \dots, m \\
& h_j(\mathbf{x}) = 0, \quad j = 1, \dots, p
\end{aligned}
$$

或微分方程形式、统计模型形式等，视具体问题而定。

## 4. Toy Model 验证

### 4.1 极简特例
取 $n = ...$、$T = ...$ 等最简参数：
- 输入：...
- 手算过程：...
- 预期输出：...

### 4.2 极端情形测试
- 当 $[\text{参数}] \to 0$ 时，模型退化为 ...，符合/不符合直觉
- 当 $[\text{参数}] \to \infty$ 时，模型表现为 ...，符合/不符合直觉

## 5. 边界条件与极限验证
- 边界 1：... → 验证结果：...
- 极限 1：... → 验证结果：...
```

---

## 模板三：公式推导-求解算法（`P3-公式推导_求解算法_[model].md`）

```markdown
# [模型名] 公式推导报告 — 求解算法

> 本推导基于 `P3-公式推导_问题建立_[model].md` 中的数学模型。

## 1. 可解性分析

### 1.1 问题类别判定
- 类别：凸优化 / 非凸优化 / ODE / PDE / 组合优化 / 其他
- 凸性分析：目标函数凸？约束集合凸？
- 解析解存在性：是 / 否 / 部分情形可解析

### 1.2 计算复杂度
- 时间复杂度上界：$O(...)$
- 空间复杂度上界：$O(...)$
-  worst-case 难度：P / NP-hard / NP-complete / 其他

## 2. 数值方法选型

### 候选方法 1：[方法名]
- 适用条件：...
- 收敛阶：...
- 优缺点：...
- 推荐理由：...

### 候选方法 2：[方法名]
- ...（同上结构）

### 最终推荐
推荐采用 **方法 X**，理由：...

## 3. 算法推导（若需）

若推荐方法需要额外推导（如梯度计算、Hessian 矩阵、对偶问题）：

**公式 (3.1)**：$$
...（如梯度公式、更新规则）
$$

## 4. 收敛性与稳定性

- 收敛条件：...
- 步长限制：...
- 迭代终止准则：...
- 初值敏感性：...

## 5. 计算资源预估

| 指标 | 估计值 | 依据 |
|:---|:---|:---|
| 单次求解耗时 | ... 秒 | 基于 $O(...)$ 与 CPU 频率估算 |
| 内存峰值 | ... GB | 基于矩阵维度与稀疏度估算 |
| 磁盘 I/O | ... MB | 基于数据规模估算 |
```

---

## 模板四：误差与敏感性分析（`P3-误差与敏感性分析_[model].md`）

```markdown
# [模型名] 误差与敏感性分析报告

> 本分析基于 `P3-公式推导_问题建立_[model].md` 与 `P3-公式推导_求解算法_[model].md`。

## 1. 误差预算表

| 误差来源 | 估计量级 | 对最终结果的影响 | 控制方法 |
|:---|:---|:---|:---|
| 模型简化误差 | $O(...)$ | ... | ... |
| 数值离散误差 | $O(...)$ | ... | ... |
| 数据测量噪声 | $O(...)$ | ... | ... |
| 舍入误差 | $O(...)$ | ... | ... |

## 2. 参数敏感性分析

### 2.1 敏感性矩阵

| 参数 $\theta_i$ | $\partial F / \partial \theta_i$ 或 $\Delta F / \Delta \theta_i$ | 敏感度评级 | 标定优先级 |
|:---|:---|:---|:---|
| ... | ... | 高/中/低 | 先/后 |

### 2.2 敏感参数分析
- 敏感参数（变化 > 10% 即显著）：...
- 钝感参数（变化 < 10% 仍不显著）：...
- 参数交互效应：...（若存在）

## 3. 结构敏感性

### 3.1 假设放松后的模型退化
- 去掉 [假设 X] → 模型退化为 ...
- 去掉 [假设 Y] → 模型退化为 ...

### 3.2 与备选方案的衔接
- 若 [数学条件，如 Hessian 不正定] 发生 → 降级到 model-architect 方案 [编号]
- 若 [数学条件，如数据充足且非线性显著] 发生 → 升级到 model-architect 方案 [编号]

## 4. 鲁棒性结论
- 模型在哪些参数区间是稳健的？
- 哪些场景下模型可能失效？
```

---

## 模板五：验证标准（`P3-验证标准_[model].md`）

```markdown
# [模型名] 验证标准

> 本文件为 quality-inspector 提供量化验收依据。

## 1. 拟合/预测性能指标

| 指标 | 符号 | 计算公式 | 可接受阈值 | 优秀阈值 |
|:---|:---|:---|:---|:---|
| 拟合优度 | $R^2$ | $1 - \frac{SS_{\text{res}}}{SS_{\text{tot}}}$ | $> 0.70$ | $> 0.90$ |
| 均方根误差 | RMSE | $\sqrt{\frac{1}{n}\sum(y_i - \hat{y}_i)^2}$ | $< 5\%$ | $< 2\%$ |
| ... | ... | ... | ... | ... |

## 2. 残差检验标准

- 残差分布：应近似 $N(0, \sigma^2)$
- 检验方法：Shapiro-Wilk / Kolmogorov-Smirnov / QQ 图
- 通过阈值：$p > 0.05$ 或 QQ 图近似直线

## 3. 敏感性阈值

- 参数扰动 10% 时，输出变化应 $< 15\%$
- 输入数据加入 5% 高斯噪声时，决策变量变化应 $< 10\%$

## 4. 计算性能阈值

- 单次求解耗时 $< ...$ 秒（在标准 PC 上）
- 内存占用 $< ...$ GB

## 5. Toy Model 验证结果

- 极简特例手算结果：...
- 数值实现结果：...
- 偏差：...（应 $< \varepsilon_{\text{tol}}$）

## 6. 交叉验证要求（若适用）

- K-Fold 折数：$K = ...$（依据样本量给出理由）
- 训练/测试划分比例：...
- 重复实验次数：...
```
